spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test?serverTimezone=Asia/Seoul
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
#        format_sql: true
#        default_batch_fetch_size: 100 # max : 1000

#logging:
#  level:
#    org.hibernate.SQL: debug
#    org.hibernate.type: trace
